## Requirements Coverage (from Plan calcfast-norm-drift-guard (qx-53v))

| # | Requirement | Status | Evidence |
|---|-------------|--------|----------|
| AC1 | `renormalize: true` option in `run/2` opts | MET | `simulation.ex:82` `resolve_renormalize/1`; `to_renorm(true)` → `:measurement` at `simulation.ex:112`; validated via `Validation.validate_renormalize!/1` at `validation.ex:329-331` |
| AC2 | Norm assertion available in test/dev mode | MET | `simulation.ex:21` `@assert_norm = Application.compile_env(:qx, :assert_norm, false)`; `assert_norm/1` at `simulation.ex:310-313`; `config/test.exs:10` sets `true`; `config/config.exs:30` sets `false` (dead code in prod, Iron Law Nx #5 satisfied) |
| AC3 (amended) | 100-gate circuit with `renormalize: 10` stays within `1.0e-6` AND renorm yields strictly lower drift than `:off` (relative-improvement guarantee) | MET | **Guard test** (`simulation_renormalization_test.exs:85-94`): 100-gate no-renorm raises `Qx.StateNormalizationError` proving drift >1e-6; **100-gate renorm test** (`test.exs:65-68`): `dev(result.state) <= 1.0e-6` with `renormalize: 10`; **60-gate direct comparison** (`test.exs:70-83`): `renormed < off` numeric assertion with both values measurable and below guard threshold — STRONGER than round 1 (replaces vacuous P4-T5 with genuine numeric comparison) |
| AC4 | No perf regression on short circuits (`:off` path within benchmark noise) | UNCLEAR | Default `:off` path is structurally unchanged (`simulation.ex:294,304`); `maybe_gate_renorm/3` catch-all and `maybe_measurement_renorm/2` `:off` clause add zero work. `AC4/non-breaking` test at `test.exs:97-112` verifies correctness equivalence but no benchmark assertion exists. Performance intent is satisfied by code structure; runtime benchmark not part of diff. |
| DC1 | `Qx.CalcFast` MUST NOT be modified | MET | `lib/qx/calc_fast.ex` not touched — renorm seam is `Simulation` + `Math` + `Validation` only |
| DC2 | Renorm reuses `Qx.Math.normalize/1`; guard reuses `Qx.Validation.validate_normalized!/2`; no new hex dep | MET | `simulation.ex:295` calls `Math.normalize/1`; `simulation.ex:311` calls `Validation.validate_normalized!/2`; `math.ex:61` has `defn normalize`; no new deps in `mix.exs` |
| DC3 | `:renormalize` resolves to `:off \| :measurement \| {:every, n}`; default `false`/`:off` zero-cost; non-breaking; CHANGELOG `[Unreleased]` only, no `mix.exs` version bump | MET | `simulation.ex:111-113` `to_renorm/1` clauses; `CHANGELOG.md:8` `[Unreleased]` section present; `mix.exs:7` version remains `"0.7.1"` (no bump) |
| DC4 | Invalid `:renormalize` raises typed `Qx.OptionError` (Iron Law #7) | MET | `validation.ex:333-335` raises `Qx.OptionError`; tests at `test.exs:121-141` cover negative int, zero, float, atom — all assert `Qx.OptionError` |
| DC5 | Guard compile-time gated via `Application.compile_env(:qx, :assert_norm, false)`; on in `:test`, off in `:prod`/`:dev`; host sync dead code in prod | MET | `simulation.ex:21` compile env read; `config/config.exs:30` `false`; `config/test.exs:10` `true`; `assert_norm/1` body only executes when `@assert_norm` is true (compiled-out in prod) |
| DC6 | Both `execute_circuit/2` (non-conditional) AND `execute_single_shot/2` (conditional) honour `:renormalize`; c_if sub-gates covered (W1 fix) | MET | **Non-conditional path**: `execute_circuit/2` at `simulation.ex:263` uses `apply_gate_step/5` which calls `maybe_gate_renorm` + `assert_norm` per gate. **Conditional timeline path**: `execute_single_shot/2` at `simulation.ex:483` threads `count` through `process_timeline_item/6`→`apply_gate_step/5` at `simulation.ex:590`. **c_if sub-gates (W1)**: `process_conditional/8` at `simulation.ex:604-617` reduces sub-instructions through `apply_gate_step/5` with the shared counter — genuine fix, not bypassed. **New conditional tests** at `test.exs:143-173`: (1) pre-measure drift (timeline path) and (2) in-c_if-block drift (W1 regression) both assert guard fires without renorm and succeeds with `renormalize: 10`. Round 1 vacuous test is replaced — now genuinely tested. |

**Summary**: 10 MET · 0 PARTIAL · 0 UNMET · 1 UNCLEAR

> AC4 UNCLEAR: performance non-regression is structurally sound (`:off` path is a no-op catch-all) but there is no benchmark assertion in the diff — manual `mix bench` verification is the appropriate check. This is not a code gap; it is a verification-mode limitation.
