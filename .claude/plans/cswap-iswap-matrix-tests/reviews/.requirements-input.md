# Requirements: qx-uos (via plan cswap-iswap-matrix-tests)

Source: `.claude/plans/cswap-iswap-matrix-tests/plan.md` (explicit arg).
Legacy issue qx-uos (bd deprecated, read-only).

## Acceptance criteria (qx-uos)

1. Test asserts `Gates.cswap()` matrix equals reference **exactly**.
2. Test asserts `Gates.iswap()` matrix equals reference with **+i** convention.
3. Convention documented in moduledoc with citation.
4. Both tests run in CI (i.e. in `mix test`).

## Plan-scoped requirements (the 4 phases)

- Phase 1: `Gates.cswap/4` normalized to `:c64` `{2ⁿ,2ⁿ}` (mirror `iswap/3`);
  doctest `gates.ex:484` `{8,8,2}`→`{8,8}`; no other caller depends on
  `{8,8,2}` (only the doctest).
- Phase 2: convention + OpenQASM 3.0 / Qiskit citation added to BOTH
  `gates.ex` `iswap/3` `@doc` and `cswap/4` `@doc` (names only, no URL).
- Phase 3: new file `test/qx/cswap_iswap_matrix_test.exs` with
  `assert_complex_matrix_equal/3` (shape-first, entrywise delta 1.0e-12,
  EXACT not global-phase-tolerant); CSWAP 8×8 exact ref; second permuted
  CSWAP case (`cswap(2,0,1,3)`, wrong-control guard); iSWAP 4×4 exact ref
  + explicit +i sign guard; negative-control identity sanity. `@moduledoc`
  documents convention (criterion #3 second location). Existing
  `cswap_gate_test.exs`/`iswap_gate_test.exs` NOT modified (TDD rule #2).
- Phase 4: full gate green; rest is human-gated merge workflow.

## Files changed this session (review scope)

- `lib/qx/gates.ex` (modified — cswap/4 body + cswap/4 & iswap/3 @doc + doctest)
- `test/qx/cswap_iswap_matrix_test.exs` (NEW)

Do NOT deep-analyze the HEAD~5 files from the prior (already-merged)
u-gate-convention session: AGENTS.md, CHANGELOG.md, ROADMAP.md, lib/qx.ex,
lib/qx/hardware*, lib/qx/operations.ex, mix.exs, test/qx/hardware*,
test/qx/u_gate_convention_test.exs.
